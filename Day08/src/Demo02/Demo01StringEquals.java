package Demo02;
/**
 * == 是进行比较对象的地址值比较，如果确实需要字符串的内容比较，可以使用两个方法；
 *
 * public boolean equals(Object obj):参数可以是任何对象，只有参数是一个字符串并且内容相同才会给true;否则返回false.
 * 备注：任何对象都能用Object进行接受。
 */
public class Demo01StringEquals {
}
